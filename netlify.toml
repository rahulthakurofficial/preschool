[build]
  base = "frontend"  # Set the base directory if your frontend is in a subfolder
  command = "npm run build"
  publish = "dist"  #  Corrected path

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
